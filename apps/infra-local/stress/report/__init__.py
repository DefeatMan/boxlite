"""Rendering what the run produced. Computes no verdicts of its own.

Empty of re-exports for the same reason as `run/__init__.py`: the run assembles
the report, and the report reads the run's verdict types.
"""
